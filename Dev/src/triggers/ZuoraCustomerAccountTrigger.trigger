trigger ZuoraCustomerAccountTrigger on Zuora__CustomerAccount__c (after delete, after insert, after undelete, after update, before delete, before insert, before update) {
   // new ZuoraCustomerAccountTriggerHandler().run();
}